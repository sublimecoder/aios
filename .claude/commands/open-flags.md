---
description: Standalone sweep of ⚠️/OPEN markers across project notes (Mode B) — aged flags are dispositioned into AIOS/History/audits/flag-register.md, never into the Log.
disable-model-invocation: true
---

Read and follow **Mode B** of `AIOS/Skills/open-flags.md` — the standalone sweep.

Arguments: `$ARGUMENTS` — an optional layer (`creator` / `professional` / `work`) or a
project name to scope the sweep. One layer at a time; never synthesize across layers.

Mode A already runs inside every `/aios-ingest` (step 1e) and is bounded by that run's
evidence. This is the other half: the flags no ingest's evidence happens to touch.

Rows go to `AIOS/History/audits/flag-register.md`, **not** the Log — one Log line per
aged flag per run would refill the file `log-rollup` exists to keep lean.
