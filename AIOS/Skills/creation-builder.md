---
tags: [aios, skill]
system: AI OS Autobuilder
trigger: "build creation rules" / "update the vault map (creation)"
---
# creation-builder

**Purpose:** build the 2nd half of the [[Vault Map]] — how AI should create notes (where files go, what templates to use).

**Steps:**
1. Define default location for each note type (capture, reference, daily, review, content draft, AI log).
2. Point to templates in `x/Templates/` and say when to use each.
3. Spell out frontmatter standards and naming conventions.
4. Update the creation sections of [[Vault Map]].

**Dependencies:** `x/Templates/`. **Output:** `AIOS/Maps/Vault Map.md`.
