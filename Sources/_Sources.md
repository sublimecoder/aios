---
tags: [aios, index]
---
# _Sources

Immutable curated inputs. **Read-only.** The AI reads these and never edits or
deletes them — the `vault-write-guard` hook blocks writes here.

Partitioned by scope: `Sources/<scope>/`. Wiki notes summarize these and point
back via the `sources:` frontmatter field. Catalog: [[Knowledge Map]].

Drop a file in, then ask the AI to ingest it.
